import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
from scipy.signal import savgol_filter
from scipy.fft import fft, fft
data = pd.read_csv('../data/spectrum.csv')

x = data['Wavelength']
y = data['Intensity']

print(data.he
plt.figure(figsize=(10,5))
plt.plot(x, y)

plt.title('Исходный спектр')
plt.xlabel('Длина волны')
plt.ylabel('Интенсивность')

plt.grid(True)
pl
filtered = savgol_filter(y, 11, 3)
plt.figure(figsize=(10,5))

plt.plot(x, y, label='Исходный')
plt.plot(x, filtered, label='Фильтрованный')

plt.legend()
plt.grid(True)

plt.savefig('../results/filtered_signal.png')


N = len(filtered)
T = 1.0

spectrum = fft(filtered)
frequencies = fftfreq(N
plt.figure(figsize=(10,5))

plt.plot(
    frequencies[:N//2],
    np.abs(spectrum[:N//2])
)

plt.title('Спектр частот')

plt.grid(True)

plt.savefig('../results/spectrum_plot.png')

plt.show(), T)plt.show()t.show()ad())freq